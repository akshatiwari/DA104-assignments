#include <stdio.h>

int main(void)
{

    int a, b;
    printf("Enter the value of A: ");
    scanf("%d", &a);

    printf("Enter the value of B: ");
    scanf("%d", &b);

    printf("A = %d and B = %d\n", a, b);
    return 0;
}